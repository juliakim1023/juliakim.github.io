# Images

Put your professional headshot here as **`headshot.jpg`** (a square image works
best, e.g. 600x600 px or larger).

Then open `_data/cv.yml` and set:

```yaml
  photo: "assets/img/headshot.jpg"
```

Commit and push, and the photo will appear in the hero section of the site.
